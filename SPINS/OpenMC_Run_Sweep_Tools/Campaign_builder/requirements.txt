# OpenMC Parametric Campaign — Python dependencies
# pip install -r requirements.txt
# Note: openmc must be installed via conda or from source (not on PyPI)

openmc>=0.14.0
numpy>=1.24.0
matplotlib>=3.7.0
pyyaml>=6.0
pandas>=2.0.0
